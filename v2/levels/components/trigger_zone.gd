## Volumetric Area3D zone that fires `entered` / `exited` for PlayerEntity bodies.
## Used by tutorial PROP_BOUNDED lessons (e.g. "stoppie zone") and any future
## gamemode prop that needs an in/out region check.
class_name TriggerZone extends GameModeObject
